{% extends 'base.html.twig' %}

{% block title %}Последний результат теста!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Последний результат теста!</h1>
    {% if viewData is not defined %}
        <h2>Тест не был пройден!</h2>
    {% else %}
        <h2>Количество вопросов на которые были получены все верные ответы: {{ viewData.correctQuestionsCount }}/{{ viewData.totalQuestionsCount }}</h2>
        {% if viewData.correctQuestionsCount > 0 %}
            <h3>Список:</h3>
            <ul>
            {% for question in viewData.correctQuestions %}
                <li>{{ question.getContent() }}</li>
            {% endfor %}
            </ul>
        {% endif %}

        <hr>

        <h2>Количество вопросов на которые не были получены все верные ответы: {{ viewData.incorrectQuestionsCount }}/{{ viewData.totalQuestionsCount }}</h2>
        {% if viewData.incorrectQuestions > 0 %}
            <h3>Список:</h3>
            <ul>
            {% for question in viewData.incorrectQuestions %}
                <li>{{ question.getContent() }}</li>
            {% endfor %}
            </ul>
        {% endif %}
    {% endif %}

    <a href="{{ path('app_math_test') }}">Пройти новый тест.</a>
</div>
{% endblock %}
